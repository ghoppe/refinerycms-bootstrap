<div id="records" class="span8 <%= ' one_list' if @recent_activity.empty? or @recent_inquiries.empty? %>">
  <% cache_if(Refinery::Pages.cache_pages_backend, [Refinery::Core.base_cache_key, "pages_backend", Globalize.locale].join('_')) do %>
    <%= render 'records' %>
  <% end %>
</div>

<div id="actions" class="span4">
  <h3><%= t('.quick_tasks') %></h3>
  <%= render 'actions' %>
</div>
